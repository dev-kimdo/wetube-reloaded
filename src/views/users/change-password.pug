extends ../base

block content
    form(method="POST")
        input(name="", placeholder="Old Password")
        input(name="", placeholder="New Password")
        input(name="", placeholder="New Password Confirmation")
        input(value="Change Password", type="submit")